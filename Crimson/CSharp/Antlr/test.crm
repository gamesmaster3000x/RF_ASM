package main (utilities(utilities.crm) utils, another(something.crm) other) {

    global bool boolean = utils.func(in, stuff);
    global int integer;

    structure person {
        int id;
        int age;
    }
    
    function main null (bool in, int otherIn) {

        int value;
        int otherValue = another.doStuff(in, otherIn);

        if (true) {
            return main(value, otherValue);
        } elif (person.id == 5) {
            return person.age;
        } else {
            return;
        }
        
    }
    
}