{
	#using "file://native.crimson/heap.crm" as heap;
	
	function pwr (number<int_size>, exponent<int_size>) {
		while (exponent <<> 0)
		{
			number = number * number<int_size>;
			exponent = exponent - 1<int_size>;
		}
	}
}