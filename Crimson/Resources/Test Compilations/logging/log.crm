{
	#using "${NATIVE}/console.crm" as utils_stdout

	// Utility package
	function multiply (num1(int_size), num2(int_size)) {
		utils_stdout.write(6);
		return num1;
	}

	function times (num1(int_size), num2(int_size)) {
		ret(int_size) = multiply(num1, num2);
		return ret;
	}
}