
#using "${NATIVE}/console.crm" as utils_stdout

// Utility package
function multiply [int] ( int num1, int num2 ) {
	utils_stdout.write(6);
    return num1 * num2;
}

function times [int] ( int num1, int num2 ) {
    return multiply(num1, num2);
}